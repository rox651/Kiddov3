---
import AllProjects from "../../views/project/AllProjects.tsx";
import { BASE_BACKEND_URL } from "../../domain/constants";
import { GetAllProjectsUseCase } from "../../usecases/project/GetAllProjectsUseCase.ts";
import Layout from "../../components/layout/Layout.astro";

const allProjects = await GetAllProjectsUseCase();
---

<Layout isDarkModeHeader isDarkModeBody isDarkModeFooter>
   <AllProjects allProjects={allProjects} client:load baseUrl={BASE_BACKEND_URL} />
</Layout>
